#include <bits/stdc++.h>
#define ll long long int

using namespace std;

int main() {

    ll n,m;
    scanf("%lld %lld",&n,&m);

    ll chance = m-1 > 0 ? m-1 : 1;
    ll ans = max(m-1,1);

    if(n - m + 1 > chance)
        ans = m+1;
    printf("%lld\n",ans);

    return 0;
}
